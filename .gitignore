# ── Credenciales Firebase (NUNCA subir a git) ──
pc/firebase_credentials.json
**/firebase_credentials.json
*serviceAccount*.json

# ── Base de datos local ──
pc/robot_bipedo.db
*.db

# ── Modelos de MediaPipe (se descargan solos la primera vez) ──
# No tiene sentido versionarlos: son pesos binarios que pc_server.py baja al
# arrancar si no están. `hand_landmarker.task` quedó commiteado de antes (7.8 MB);
# si algún día se limpia el historial, sacarlo también.
pc/efficientdet_lite0.tflite
*.tflite

# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# ── Editor / SO ──
.vscode/
.DS_Store
Thumbs.db

# ── Compilación PlatformIO / ESP-IDF (no versionar el build, pesa cientos de MB) ──
.pio/
**/.pio/
build/
**/build/
managed_components/
